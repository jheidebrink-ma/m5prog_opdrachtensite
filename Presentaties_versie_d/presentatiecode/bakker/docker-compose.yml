version: '3.9'

# Services
services:
 # PHP FPM Service
  php:
    image: wodby/php:latest
    volumes:
      - './html:/var/www/html'
# Nginx Service
  nginx:
    image: nginx:latest
    ports:
      - 88:80
    links:
      - 'php'
    volumes:
      - './html:/var/www/html'
      - './docker/nginx:/etc/nginx/conf.d'
    depends_on:
      - php